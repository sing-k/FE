import styled from "styled-components";

const RecommendBoardTitle = () => {
  return <Title>QWER 대박 추천!</Title>;
};

export default RecommendBoardTitle;

const Title = styled.p`
  font-weight: 800;
  font-size: 1.3rem;
  margin-left: 0.4rem;
  margin-bottom: 0.1rem;
  padding-bottom: 0.1rem;
`;
