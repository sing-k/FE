import styled from "styled-components";

import color from "../../../styles/color";

const ArtistName = () => {
  return <Name>아티스트명</Name>;
};

export default ArtistName;

const Name = styled.p`
  font-size: 0.8rem;
`;
